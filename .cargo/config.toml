[target.x86_64-unknown-linux-gnu]
     linker = "clang"
       rustflags = ["-C", "link-arg=-fuse-ld=mold"]

[alias]
viz = "run --features visualization -- -v"
pull = "run -- -p"
train = "run -- -t"
serve = "run -- -s"
